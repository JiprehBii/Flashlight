<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
	android:orientation="vertical"
	android:background="#BABEBABE"
	>

	<Button
		android:layout_height="wrap_content"
		android:layout_width="wrap_content"
		android:text="Led"
		android:id="@+id/ledButton"
		android:layout_marginTop="200dp"
		android:textStyle="bold"
		android:layout_marginLeft="60dp"/>

	<Button
		android:layout_height="wrap_content"
		android:layout_width="wrap_content"
		android:text="Screen"
		android:layout_toRightOf="@id/ledButton"
		android:id="@+id/scrnButton"
		android:textStyle="bold"
		android:layout_alignBaseline="@id/ledButton"
		android:layout_alignBottom="@id/ledButton"/>

	<Button
		android:id="@+id/closeButton"
		android:layout_height="wrap_content"
		android:layout_width="wrap_content"
		android:text="Close"
		android:layout_below="@id/scrnButton"
		android:layout_marginLeft="100dp"
		android:layout_marginTop="150dp"/>

	<TextView
		android:layout_height="wrap_content"
		android:text="Torch"
		android:textAppearance="?android:attr/textAppearanceLarge"
		android:layout_width="wrap_content"
		android:id="@+id/TxtView"
		android:layout_centerHorizontal="true"
		android:layout_marginTop="40dp"
		android:textColor="#CDC111"
		android:textStyle="bold|normal"
		android:shadowColor="#000000"
		android:shadowRadius="10"
		android:padding="45dp"
		android:layout_marginRight="55dp"
		android:gravity="top"
		android:textSize="40sp"/>

</RelativeLayout>
